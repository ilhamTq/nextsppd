import MainLayout from "@/components/MainLayout";
export const metadata = {
  title: "Jabatan",
};

const LayoutJabatan = ({ children }) => {
  return <MainLayout>{children}</MainLayout>;
};

export default LayoutJabatan;
