import MainLayout from "@/components/MainLayout";
export const metadata = {
  title: "Surat Perintah Perjalanan Dinas",
};

const Sppdlayout = ({ children }) => {
  return <MainLayout>{children}</MainLayout>;
};

export default Sppdlayout;
